# TG SEO Funnel (статический сайт + автопостинг в Telegram)

Проект делает ровно то, что ты описал:

1) Берёт список запросов (например, выгрузка из Wordstat) из `topics.csv`  
2) Для каждого запроса создаёт:
   - Заголовок (Title Case)
   - **slug** (кусок URL), который детерминированно зависит от заголовка
   - Статическую страницу `site/<slug>/index.html`
   - Строку в `posts.jsonl` (готовый текст поста с ссылкой на страницу)
3) Публикует посты в Telegram-канал с целевым темпом **26 постов/мин** (и корректно переживает 429 retry_after)

## Почему “без оплаты за сайт”
Это **статический сайт** (папка `site/`). Его можно бесплатно разместить на:
- GitHub Pages (самый простой вариант)
- Cloudflare Pages

## Быстрый старт

### 1) Установка
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# macOS/Linux:
# source venv/bin/activate

pip install -r requirements.txt
```

### 2) Настройки
Скопируй `.env.example` в `.env` и заполни.

Минимально нужно:
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHANNEL` (например, `@mychannel` или `-100...`)
- `SITE_BASE_URL` — адрес будущего сайта (например, `https://username.github.io/repo` или `https://project.pages.dev`)

Опционально:
- `OPENAI_API_KEY` (если хочешь генерацию текста через GPT).  
  Если ключа нет — проект всё равно работает и генерит аккуратный шаблонный текст.

### 3) Подготовь запросы
Отредактируй `topics.csv` (колонка `query`).

### 4) Сгенерировать страницы + посты
```bash
python cli.py build --limit 26
```
- Страницы появятся в `site/`
- Посты появятся в `posts.jsonl`
- `site/sitemap.xml` и `site/robots.txt` создадутся автоматически

### 5) Опубликовать 26 постов в канал за минуту
```bash
python cli.py publish --limit 26 --rate 26
```

### 6) Всё одним махом
```bash
python cli.py all --limit 26 --rate 26
```

---

## Деплой на GitHub Pages (бесплатно)

1) Создай репозиторий на GitHub и залей туда проект.  
2) В Settings → Pages включи публикацию из ветки `main` и папки `/site` (или через Actions — см. ниже).  
3) Либо используй готовый workflow: `.github/workflows/pages.yml`  
   Он будет:
   - собирать `site/` из `topics.csv`
   - публиковать на GitHub Pages

После публикации выстави `SITE_BASE_URL` под фактический адрес Pages.

---

## Важное
- Telegram может вернуть 429 (Too Many Requests). Скрипт **автоматически подождёт retry_after** и продолжит.
- Если один “пост” превысит лимит Telegram по длине, он будет разбит на несколько сообщений (это влияет на лимиты).
- Этот проект — техническая заготовка. Делай страницы полезными (уникальный текст, FAQ, примеры), чтобы не выглядело как “пустая прокладка”.

Удачи 🚀
